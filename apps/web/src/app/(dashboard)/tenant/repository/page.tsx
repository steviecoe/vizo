import { ImageRepository } from '@/components/tenant/ImageRepository';

export default function ImageRepositoryPage() {
  return (
    <div className="min-h-full bg-stone-50 p-8">
      <ImageRepository />
    </div>
  );
}
